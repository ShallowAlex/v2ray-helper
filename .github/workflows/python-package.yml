name: "v2ray-helper"

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.7
      - run: |
          pip install -r requirements.txt
<<<<<<< HEAD
          string="${{ secrets.fromdata }}"
=======
          string="${{ secrets.PW }}"
>>>>>>> 9af751a65664aad5bc5832b879781ec59c3c4ea6
          IFS="#"
          array=($string)
          for var in ${array[@]}
          do
            python3 v2helper.py <<EOF
            $var
          EOF
          done
